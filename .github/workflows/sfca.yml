name: new Salesforce Code Analyzer Workflow

on:
  pull_request:
    paths:
      - 'force-app/main/default/classes/**'
      - 'sfca-config.yml'

jobs:
  sfca:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
            fetch-depth: 0
      

      # -------------------------------
      # Install yq (YAML parser)
      # -------------------------------
      - name: Install yq
        run: |
          sudo wget -qO /usr/local/bin/yq \
            https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq

      # -------------------------------
      # Fetch base branch
      # -------------------------------
      - name: Fetch base branch
        run: git fetch origin ${{ github.base_ref }}

      # -------------------------------
      # Build final FILES list
      # -------------------------------
      - name: Resolve Apex files for SFCA
        run: |
            echo "🔹 Reading exclusions from sfca-config.yml"
            PMD_EXCLUSIONS=$(yq '.engines.pmd.fileExclusions[]' sfca-config.yml || true)
        
            echo "PMD exclusions:"
            echo "$PMD_EXCLUSIONS"
        
            FINAL_FILES="$FILES"
        
            for EXCLUDE in $PMD_EXCLUSIONS; do
            REGEX=$(echo "$EXCLUDE" | sed 's/\*\*/.*/g')
            FINAL_FILES=$(echo "$FINAL_FILES" | tr ' ' '\n' | grep -v -E "$REGEX" || true)
            FINAL_FILES=$(echo "$FINAL_FILES" | tr '\n' ' ')
            done
        
            echo "Final Apex files:"
            echo "$FINAL_FILES"
        
            echo "FILES=$FINAL_FILES" >> $GITHUB_ENV
    
        

      # -------------------------------
      # Run Salesforce Code Analyzer
      # -------------------------------
      - name: Run Salesforce Code Analyzer
        run: |
            if [ -z "$FILES" ]; then
            echo "✅ No Apex files to analyze after exclusions"
            exit 0
            fi
            
            sf code-analyzer run $FILES \
                --config-file ./sfca-config.yml \
                --view detail \
                --output-file sfca_results.html
        
