engines:
  # retirejs:
  #   fileExclusions:
  #     - force-app/main/default/lwc/Excluded/**
  #     - force-app/main/default/lwc/dmlAverage/**
      
  # pmd:
  #   custom_rulesets:
  #     - config/pmd/apex-ruleset.xml
  # eslint:
  #     auto_discover_eslint_config: true
  #     eslint_ignore_file: .eslintignore

  cpd:
    file_extensions:
      apex:
        - ".trigger"
    minimum_tokens:
      apex: 30
  pmd:
    file_extensions:
      apex:
        - ".trigger"
