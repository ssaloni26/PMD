engines:
  eslint:
    auto_discover_eslint_config: true
    eslint_ignore_file: .eslintignore

  # retirejs:
  #   disable_engine: false

  cpd:
    file_extensions:
      apex:
        - ".cls"
        - ".trigger"

  pmd:
    file_extensions:
      apex:
        - ".cls"
        - ".trigger"

  regex:
    file_extensions:
      apex:
        - ".cls"
        - ".trigger"

  # flow:
  #   disable_engine: false
