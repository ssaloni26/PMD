engines:
  pmd:
    exclude-file-list: excluded-files.txt

  cpd:
    exclude: "**/PermissionSetsAuditController.*"
    minimum_tokens:
      apex: 50

  eslint:
    eslint_ignore_file: .eslintignore

# cSpell:ignore sfca-regex-rules
  regex:
    custom_rules:
      path: ./sfca-regex-rules.yml


  retirejs:
    exclude_patterns:
      - force-app/main/default/lwc/Excluded/**
      - force-app/main/default/lwc/dmlAverage

  flow:
    exclude_paths: 
      - "**/flow.flow-meta.xml" 
